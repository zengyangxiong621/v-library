import { memo } from 'react'
import './index.less'


const DelDataSource = (props: any) => {

  return (
  <div className='DelDataSource-wrap'>
    DelDataSource
  </div>
  )
}

export default memo(DelDataSource)